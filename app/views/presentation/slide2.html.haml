%h1 Why -- what's the point Dixon?
%h2 Here's some of the things we've found useful:
%ul
	%li Separate deployments from functionality, reducing risk
	%li Get new code to master sooner, avoiding merge conflicts*
	%li Test new features on production *before* your customers do.
	%li Test new features on production *before* your customers do.

%p
	Also, this presentation needs more pop, doesn't it.  Let's enable some
	=link_to 'features!', :controller => 'features'

- if on? :better_layout
	%h3= "Hey -- make a better layout yo!!!"
